services:
  - type: web
    name: faceattend-backend
    runtime: node
    buildCommand: npm install && node download-models.js
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: ADMIN_KEY
        value: changeme123
